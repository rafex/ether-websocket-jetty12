# ether-websocket-jetty12

Jetty 12 transport adapter for `ether-websocket-core`.

## Scope

- Jetty session adapter
- Route registry primitives
- Jetty 12 upgrade handler wiring
- Server factory and runner
- Module contract for route registration

This module may depend on `org.eclipse.jetty:*`.
